# ── Ringfence core runtime ────────────────────────────────────────────────
# Pinned loosely (>=) so the repo installs cleanly on 3.11 / 3.12 / 3.13.
numpy>=1.26
pandas>=2.1
networkx>=3.2
scikit-learn>=1.4
lightgbm>=4.3
python-louvain>=0.16          # Louvain community detection (imports as `community`)

# ── API layer ─────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27                   # also used by fastapi.testclient

# ── Demo dashboard ────────────────────────────────────────────────────────
streamlit>=1.33
plotly>=5.20

# ── Persistence ───────────────────────────────────────────────────────────
joblib>=1.3
# sqlite3 is stdlib

# ── LLM dossier summary (optional — system degrades to deterministic-only) ─
openai>=1.20

# ── Tests / notebooks ─────────────────────────────────────────────────────
pytest>=8.0
matplotlib>=3.8
ipykernel>=6.29             # so notebooks/ are runnable straight after `make setup`

# ── OPTIONAL heavy extras ─────────────────────────────────────────────────
# Case memory uses a pure-numpy cosine index by default so `make test` runs
# without a ~2GB torch download. Install these to switch on the ChromaDB
# vector backend (auto-detected at import time, see src/case_memory.py):
#   pip install "chromadb>=0.5" "sentence-transformers>=2.7"
